<Grid
    x:Class="UnitedSets.UI.Controls.WindowHoverIndicatorBackground"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnitedSets.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid.Resources>
        <Storyboard x:Name="WindowHoveringStoryBoard">
            <ColorAnimation Storyboard.TargetName="FlashingBackgroundColor"
                Storyboard.TargetProperty="Color"
                Duration="0:0:0.5"
                From="Transparent"
                To="{ThemeResource LayerFillColorDefault}">
                <ColorAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </ColorAnimation.EasingFunction>
            </ColorAnimation>
        </Storyboard>
        <Storyboard x:Name="NoWindowHoveringStoryBoard">
            <ColorAnimation Storyboard.TargetName="FlashingBackgroundColor"
                Storyboard.TargetProperty="Color"
                Duration="0:0:0.5"
                From="{ThemeResource LayerFillColorDefault}"
                To="Transparent">
                <ColorAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseIn" />
                </ColorAnimation.EasingFunction>
            </ColorAnimation>
        </Storyboard>
    </Grid.Resources>
    <Grid.Background>
        <SolidColorBrush x:Name="FlashingBackgroundColor" Color="Transparent"/>
    </Grid.Background>
</Grid>
